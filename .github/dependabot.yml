# Dependabot config — OW-216 P5 (Snyk fleet dep-debt campaign)
# Activated 2026-05-30. Repo is pure PowerShell — no native PowerShell-module
# ecosystem in Dependabot; only github-actions is scannable.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
